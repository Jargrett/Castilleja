emotes::install_version("Matrix", version = "1.6.3")
remotes::install_version("Matrix", version = "1.6.3")
install.packages("lme4", type = "source")  # 1.1.35.1
remotes::install_version("Matrix", version = "1.6.4")
library(lme4)
library(metagear)
setwd("/Users/jargrett/Desktop/Castilleja/Data Analysis/RMBL/2024 Argrett")
remotes::install_github("cornwell-lab-unsw/litterfitter")
library(litterfitter)#for k-curve fitting
library(tidyverse)#for data wrangling and restructuring
library(plyr)#for data wrangling and restructuring
library(lme4)#for modeling linear mixed effect models
library(nlme)#alternative for modeling linear mixed effect models
library(ggplot2)#for plotting
library(ggpubr)#extended functions for plotting
library(car)#for regression analysis
library(emmeans)#post-hoc analysis
#load-in the data
winter <- read.csv("Litter Decomposition - 2023-2024 Overwinter.csv")
View(winter)
#check structure
str(winter)
#load-in the data
winter <- read.csv("Litter Decomposition - 2023-2024 Overwinter.csv")
#check structure
str(winter)
winter$litter <- as.factor(winter$litter)
winter$removal <- as.factor(winter$removal)
winter$deployment_duration <- as.factor(winter$deployment_duration)
#remove missing bags
winter <- winter %>% drop_na(missing)
#load-in the data
winter <- read.csv("Litter Decomposition - 2023-2024 Overwinter.csv")
#remove missing bags
winter <- winter %>% drop_na(missing)
View(winter)
#remove missing bags
winter <- winter %>%
filter(missing = "yes")
#remove missing bags
winter <- winter %>%
filter(missing == "yes")
View(winter)
#remove missing bags
winter <- winter %>%  filter(missing!="yes")
#load-in the data
winter <- read.csv("Litter Decomposition - 2023-2024 Overwinter.csv")
#remove missing bags
winter <- winter %>%  filter(missing!="yes")
View(winter)
#remove missing bags
winter <- winter %>%  filter(missing=="yes")
#remove missing bags
winter <- winter %>%  filter(missing="yes")
#remove missing bags
winter <- winter %>% filter(missing == yes)
#load-in the data
winter <- read.csv("Litter Decomposition - 2023-2024 Overwinter.csv")
#remove missing bags
winter <- winter %>% filter(missing == yes)
#check structure
str(winter)
#remove missing bags
winter <- winter %>% filter(missing == "Yes")
View(winter)
#load-in the data
winter <- read.csv("Litter Decomposition - 2023-2024 Overwinter.csv")
#check structure
str(winter)
winter$litter <- as.factor(winter$litter)
winter$removal <- as.factor(winter$removal)
winter$deployment_duration <- as.factor(winter$deployment_duration)
#remove missing bags
winter <- winter %>% filter(missing != "Yes")
View(winter)
#remove missing bags
winter <- winter %>%
filter(missing != "Yes") %>%
select(-c((missing)))
View(winter)
View(winter)
#first lets see if decomp is different between treatment
over.lmm <- lmer(mass_loss ~ litter*removal, data = winter)
#first lets see if decomp is different between treatment
over.lmm <- lmer(mass_loss ~ litter*removal + (1|location), data = winter)
summary(over.lmm)
Anova(over.lmm)
emmip(over.lmm, litter ~ mass_loss)
emmeans(over.lmm, pairwise ~ litter)
#first lets see if decomp is different between treatment
over.lmm <- lmer(final_dry_weight ~ litter*removal + (1|location), data = winter)
summary(over.lmm)
Anova(over.lmm)
emmip(over.lmm, litter ~ removal)
emmeans(over.lmm, pairwise ~ litter)
#first lets see if decomp is different between treatment
over.lmm <- lmer(final_dry_weight ~ litter*removal + (1|location), data = winter)
summary(over.lmm)
Anova(over.lmm)
emmip(over.lmm, removal ~ litter)
emmeans(over.lmm, pairwise ~ litter)
#first lets see if decomp is different between treatment
over.lmm <- lm(final_dry_weight ~ litter, data = winter)
summary(over.lmm)
Anova(over.lmm)
emmip(over.lmm, removal ~ litter)
emmeans(over.lmm, pairwise ~ litter)
#first lets see if decomp is different between treatment
over.lm <- lm(final_dry_weight ~ removal, data = winter)
summary(over.lm)
Anova(over.lm)
